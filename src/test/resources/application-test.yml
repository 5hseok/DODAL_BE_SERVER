spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MYSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  security:
    oauth2:
      apple:
        grant_type: testGrantType
        redirect-uri: testRedirectUri
        key-id: testKeyId
        public-key-uri: https://appleid.apple.com/auth/oauth2/v2/keys
        client:
          client-id: testClientId
          team-id: testTeamId
          client-secret: testClientSecret

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

  sql:
    init:
      mode: never

logging:
  level:
    root: DEBUG

server:
  port: 9999

jwt:
  secret: testsecretcodeisrandomvalueokaksdfopqjksdnklfjpqjklsdjojpoaklqnknn123nlknlkn1lnodnjkv
  access-token-validity-in-milli-seconds: 3600000
  refresh-token-validity-in-milli-seconds: 604800000
jasypt:
  encryptor:
    password: testpassword
